# Software Dependencies

This inventory is based on the supplied source files. It may omit indirect dependencies, data services or external commands. Original software versions are not pinned.

## R

Packages identified: `DT`, `FactoMineR`, `RMariaDB`, `bslib`, `dplyr`, `factoextra`, `ggplot2`, `lubridate`, `plotly`, `questionr`, `readr`, `scales`, `shiny`, `stats`, `tidyr`, `tidyverse`.

## Additional requirements

Quarto is needed to render the `.qmd` file. The SQL component requires authorised MariaDB access and compatible client libraries. The Shiny application requires its prepared local data object.

See [the project README](README.md) for expected inputs and [reproducibility notes](../../REPRODUCTIBILITE.md) for execution limits.
